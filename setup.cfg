[metadata]
name = ai_crop_images
version = 0.1.0

[options]
packages = find:
install_requires = file: requirements.txt

[options.entry_points]
console_scripts = ai_crop_images = ai_crop_images.main:cli 
